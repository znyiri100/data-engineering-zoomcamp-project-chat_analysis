name: chat_analysis_2026_dataset.ingestion_topic_lookup
type: bq.seed

parameters:
  path: topic_lookup.csv

depends:
  - upload_to_gcs

columns:
  - name: topic_id
    type: integer
    primary_key: true
    description: Unique identifier for the topic
  - name: topic
    type: string
    description: The human-readable name of the topic
